package com.duuolf.launcher.ui.theme

import androidx.compose.ui.graphics.Color

// 浅色模式（白底黑字）
val LightBackground = Color(0xFFFFFFFF) // 纯白背景
val LightForeground = Color(0xFF000000) // 纯黑前景
val LightCard = Color(0xFFE0E0E0)       // 浅灰色卡片
val LightCardBorder = Color(0xFF757575) // 深灰色边框
val LightCardColorOptimized = Color(0xFFF5F5F5) // 纯白，增强清晰感
val LightCardBorderOptimized = Color(0xFF616161) // 更深的灰色边框，清晰但不刺眼

// 深色模式（黑底白字）
val DarkBackground = Color(0xFF000000)  // 纯黑背景
val DarkForeground = Color(0xFFFFFFFF)  // 纯白前景
val DarkCard = Color(0xFF212121)        // 深灰卡片
val DarkCardBorder = Color(0xFFBDBDBD)  // 亮灰色边框
val DarkCardColorOptimized = Color(0xFF212121) // 更深的灰色，增强对比度
val DarkCardBorderOptimized = Color(0xFF888888) // 适中的亮灰色边框